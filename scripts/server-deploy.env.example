# 复制本文件为 scripts/server-deploy.env 后再按需修改

# master 访问地址仅用于脚本提示，真正监听端口仍由 config.yaml 控制
MASTER_HOST=0.0.0.0
MASTER_PORT=8080

# agent 启动参数
AGENT_PORT=8089
AGENT_DATA_DIR=/opt/jmeter/csv-data
AGENT_TOKEN=
AGENT_JMETER_PATH=/opt/apache-jmeter-5.6.3/bin/jmeter
